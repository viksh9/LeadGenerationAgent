"""Operational CLI package (Prompt 39).

Run with ``python -m app.audit <command>``. Provides real-data-quality and
production-readiness audits that report ACTUAL counts and fail closed on real
violations — they never fabricate data or pass over synthetic contamination.
"""
