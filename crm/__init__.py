"""Internal CRM & sales lifecycle (Prompt 39).

Real-data-only lead management: lifecycle state machine, status history, CRM
activities, sales pipeline, follow-ups, and honest analytics. This package never
fabricates outreach history, replies, meetings, revenue, or conversion metrics —
a fact is recorded only when a real human action or provider event supports it.
"""
