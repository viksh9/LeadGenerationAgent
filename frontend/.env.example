# Base URL of the FastAPI backend. Anything prefixed with VITE_ is exposed to the
# browser — never put secrets here.
VITE_API_BASE_URL=http://localhost:8000

# Optional request timeouts in milliseconds.
# VITE_API_TIMEOUT=15000
# VITE_ANALYZE_TIMEOUT=30000
